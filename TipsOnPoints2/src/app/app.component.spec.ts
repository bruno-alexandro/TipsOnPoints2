import { TestBed } from '@angular/core/testing';
// import { DataDisplayComponent } from './app.component';

describe('AppComponent', () => {
  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [DataDisplayComponent],
  //   }).compileComponents();
  // });

  // it('should create the app', () => {
  //   const fixture = TestBed.createComponent(DataDisplayComponent);
  //   const app = fixture.componentInstance;
  //   expect(app).toBeTruthy();
  // });

  // it(`should have the 'TipsOnPoints2' title`, () => {
  //   const fixture = TestBed.createComponent(DataDisplayComponent);
  //   const app = fixture.componentInstance;
  //   expect(app).toEqual('TipsOnPoints2');
  // });

//   it('should render title', () => {
//     const fixture = TestBed.createComponent(DataDisplayComponent);
//     fixture.detectChanges();
//     const compiled = fixture.nativeElement as HTMLElement;
//     expect(compiled.querySelector('h1')?.textContent).toContain('Hello, TipsOnPoints2');
//   });

});
